<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jeckliu.multimedia">

    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <uses-feature android:name="android.hardware.Camera"/>
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application android:allowBackup="true" android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".MultimediaActivity"/>
        <activity android:name=".clip.VideoSelectActivity"/>
        <activity android:name=".clip.VideoClipActivity"/>
        <activity android:name=".shoot.ShootActivity" />
    </application>

</manifest>
